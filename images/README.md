Place node images in this folder.

Usage in node `icon` field:
- `images/my-icon.png`
- `images/my-badge.svg`

You can still use emoji/text icons (for example `✦`, `☎`, `👥`) in the same field.
